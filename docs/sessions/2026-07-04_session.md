# Session Log — 2026-07-04

**Project:** Project_006_JuniorSE_Agent (+ Project_005 SAP2000 API)
**Engineer:** yahtrhee50-ship-it

---

## 1. FEA Phase 1: Frame moment releases + rigid end offsets — COMPLETE

**Commit (P006):** `9d7f987` (pushed to master). Also committed leftover session-log +
statusline diff as `3498638`.

- `src/fea/elements/frame.py`:
  - **Moment releases** (`release_i`/`release_j`): static condensation of released rz DOFs
    out of the local 6x6 stiffness AND the equivalent-load vector `_q0`. Released end
    carries exactly zero moment; interior element loads redistribute consistently.
  - **Rigid end offsets** (`offset_i`/`offset_j`, GLOBAL node→flexible-end vectors):
    per-node rigid-link transform W (`u_end = u_node + rz × offset`); element
    length/orientation computed from the OFFSET end coordinates; K = (TW)ᵀ k_cond (TW).
- `src/fea/api.py`: `Model.element("Frame", ...)` passes through the four new kwargs.
- `tests/fea/test_frame_releases_offsets.py` — 9 tests, all closed-form-exact (rtol 1e-9):
  propped cantilever via release (3wL/8, 5wL/8, wL²/8), double release = simple beam,
  Gerber fixed-hinge-roller by statics (60/160/20), midspan-P propped cantilever
  (11P/16, 5P/16, 3PL/16), rigid zone at support (P·Lf³/3EI, M=P·L), rigid zone at loaded
  tip, defaults regression, FD tangent w/ loads, symmetry + rank (1 release → 4 zero modes).
- **Gotcha:** a 2-node model with both ends fully fixed has zero free DOFs → engine
  raises "All DOFs are constrained" — tests need a free midspan node.
- **Repo total: 211 tests, all green** (was 202).

## 2. NEW GOAL (user, session-scoped): add SAP2000 features, modularized for later tool conversion

**Commit (P005):** `9f7247a` (pushed to master).

### Pattern established — `services/sap2000/operations/`
- Every capability = standalone `func(conn, **json_params) -> json_dict`, registered in
  `OPERATIONS` dict in `operations/__init__.py`.
- One generic route dispatches by name: `POST /api/sap2000/op/{name}` (body = kwargs JSON);
  `GET /api/sap2000/op` lists operations + first docstring lines.
- Adding a new operation = 1 function + 1 registry entry. Each function converts 1:1 into
  an MCP tool later (that's the design intent — junior-se server can wrap them).

### First feature batch: analysis-results extraction (`operations/results.py`)
`list_load_cases`, `joint_reactions`, `joint_displacements`, `frame_forces`,
`base_reactions`, `modal_periods`.

**COM tuple layouts verified live** (probe scripts, session scratchpad):
- comtypes returns all ByRef params as a tuple, **ret code LAST** (`r[-1]`, 0=ok).
- `JointReact`/`JointDispl`: r[0]=n, 1=Obj, 2=Elm, 3=LoadCase, 4=StepType, 5=StepNum,
  6–11 = F1,F2,F3,M1,M2,M3 (or U1..R3), 12=ret.
- `FrameForce` (13 placeholder lists after Name+ItemType!): r[0]=n, 1=Obj, 2=ObjSta(numeric),
  3=Elm, 4=ElmSta, 5=LoadCase, 6=StepType, 7=StepNum, 8–13 = P,V2,V3,T,M2,M3, 14=ret.
- `BaseReact`: r[0]=n, 1=LoadCase, 2=StepType, 3=StepNum, 4–9 = FX..MZ, 10–12 = gx,gy,gz, 13=ret.
- `ModalPeriod`: r[0]=n, 1=LoadCase, 2=StepType, 3=StepNum(mode), 4=Period, 5=Freq,
  6=CircFreq, 7=EigenValue, 8=ret.
- `LoadCases.GetNameList` / `GroupDef.GetNameList`: (n, names, ret).
- `Analyze.GetCaseStatus`: (n, names, statuses, ret); status 4=finished, 1=not run.
- Group query: `("ALL", ItemTypeElm=2)` works (group "ALL" always exists); joint queries
  by name use ItemTypeElm=0.

### Silent-failure guard added (important!)
`connector.run_analysis()` now calls `GetCaseStatus` after `RunAnalysis` and **raises if no
case finished**. Observed: a SAP2000 instance orphaned by a killed COM client accepts all
build/save calls and returns 0 from RunAnalysis **without running anything** (model never
locks, all Results.* return ret=1/0 rows). Restarting SAP2000 fixed it.

### Verification (real integration tests, run this session)
- `scripts/test_results_operations.py`: **12/12 PASS** — SS beam (6 m, pin/roller,
  torsion restrained at pin — REQUIRED or the model is unstable), 10 kN midspan:
  reactions 5+5 kN, ΣF3=10, base FZ=10, max |M3| = PL/4 = 15 kN·m, max |V2| = 5 kN,
  zero support displacement, modal graceful.
- REST end-to-end: server started, `/connect` (attach), `GET /op`, `/op/joint_reactions`
  (correct 5/5 kN), `/op/base_reactions`, unknown-op 404. Server stopped after.
- `joint_reactions` ALL-path filters to restrained joints only (free joints produce
  all-zero noise rows otherwise).

## 3. P005 `define_load_combos` operation — COMPLETE (session 2, after /clear)

**Commit (P005):** `88d5e79` (pushed to master).

- `operations/combos.py`:
  - `generate_lrfd_combos(case_map)` — PURE enumerator (no COM), unit-testable.
    Expands ASCE 7-22 "or" alternatives into variants: `0.5(Lr|S|R)` → one variant per
    mapped roof type; combo 3's `(L or 0.5W)` → one variant per mapped companion.
    Names: `LC2` if single variant, else `LC2a/LC2b/...`. Skips combos whose
    distinguishing load is unmapped (no W → LC4/5, no E → LC6/7, no roof → LC3),
    returned in `skipped` with reasons. `D` is required in case_map.
  - `define_load_combos(conn, case_map, prefix="", envelope=True, replace=True)` —
    creates linear-additive `RespCombo`s + `<prefix>LRFD-ENV` envelope combo
    (RespCombo.Add type 1) over all created combos. Validates case_map names against
    model cases AND combos (a mapped name may itself be a combo). `replace=True`
    deletes same-named combos first (idempotent). Payload carries equations, code
    refs, per-case factors, and explicit assumptions (no LL reduction; companion L
    at 1.0 — 0.5L exception not taken; W/E reversal = engineer's job, map directional
    cases or call per direction with a `prefix`).
- COM notes: `RespCombo.Add(name, type)` returns bare int ret; `SetCaseList(name,
  cNameType, caseName, sf)` with cNameType 0=case 1=combo returns tuple, ret last.
  `_check` in combos.py uses `ret_code[-1]` (results.py's uses `[0]` — fine there
  because those tuples are checked via `r[-1]` at call sites).
- **Verified live** (`scripts/test_combos_operation.py`, 25/25 PASS): pure-logic
  full-map enumeration (12 combos incl. 4 LC3 variants), D+L-only subset, missing-D
  rejection; live: combos created in SAP2000, second call idempotent, unknown case
  name rejected with model case list, and **factored reactions match statics
  exactly** — LC1: 7 kN/support (1.4·10/2), LC2: 22 kN (1.2·10+1.6·20)/2, LRFD-ENV
  max: 22 kN, on the standard 6 m SS test beam.
- REST dispatch not re-tested this session — generic `/op/{name}` mechanism was
  verified end-to-end last session; new op is just a registry entry.

## Still open / deferred
- **Next SAP2000 operations to add (same pattern):** ~~load combinations~~ DONE
  (`define_load_combos`, 88d5e79); frame releases/end offsets assignment,
  section assignment from AISC db, custom axle trains for moving load. Then: MCP tools
  in P006 wrapping `/api/sap2000/op/*`.
- FEA engine Phase 1 remainder: load combos, Tier-2 archetypes
  (SimplySupportedBeam/Cantilever/PortalFrame/ContinuousBeam), RCM numberer,
  `solve_model` MCP tool, section property tool.
- Ch15 P9 textbook discrepancy (44.2 vs 57.6 kN·m) — engineer to resolve.
- ACI 318-19 textbook benchmark; doubly-reinforced/T-beam; columns/connections;
  wind & seismic; NAFEMS paths A (dynamics) + C (plane stress) for SAP2000 adoption.
- Reference/.md/.json + template/docs files still untracked in P006 git.

## Resume point (updated after session 2)
SAP2000 left RUNNING (healthy instance; test model `test_combos_op.sdb` loaded).
`define_load_combos` is DONE. Next task, pick one:
1. Next P005 operation (same pattern): frame release/end-offset assignment, or AISC
   section assignment, or custom axle trains; OR
2. Start wrapping P005 `/api/sap2000/op/*` operations as junior-se MCP tools in P006
   (`src/mcp_server.py`) — the operations contract was designed for 1:1 conversion.

---

# Session 3 (2026-07-04, same day): FEA confirmation sweep + SAP2000 thick-shell deck check

## Goal
Confirm the FEA model against known stiffness-method scenarios, and check a thick-shell
concrete deck with beams, girders, and columns in SAP2000.

## Completed

### 1. FEA engine stiffness-method confirmation (all re-run this session)
- `pytest -q`: **211/211 green** (Hibbeler Ch14 trusses, Ch15 beams via GeneralBeam,
  Ch16 frames via fea Frame, MacNeal-Harder beam suite, frame releases/offsets,
  FD tangent checks).
- All 11 standalone Ch15 scripts (`python tests/benchmark_ch15_pN.py`): **RESULT: PASS**
  (P9 still passes vs 3-moment eq; textbook discrepancy remains pinned).

### 2. New P005 modular operations (P005 commit e24561b, pushed)
- `operations/modeling.py`: **add_columns** (vertical columns below plan positions;
  AISC W-import or rectangle, steel/concrete, fixed/pinned base; top joints auto-merge
  with deck joints), **find_joints** (closest joint to a coordinate — needed because
  SAP2000 renames joints), **run_analysis** (save + run + per-case status).
- Builder fixes: **LL pattern had AddLoadCase=False → no "LL" results case ever
  existed** (base_reactions on LL failed with "neither a load case nor a combo");
  now True. Secondary beams now report "requested -> actual" frame names (girders
  already did) so their forces are queryable.

### 3. SAP2000 thick-shell validation (P006 `scripts/check_thick_shell_deck.py`) — 18/18 PASS
- **Phase A — known scenario:** 6 m simply supported square concrete plate
  (thick-shell, t=0.15 m, 12x12 mesh @0.5 m, pins on all perimeter grid joints,
  q=10 kN/m2). Sum reactions LL/DEAD exact (360.00 / 129.60 kN). Center deflection
  7.336 mm vs Kirchhoff 0.00406*q*a^4/D = 7.222 mm → **ratio 1.0158** (Mindlin
  shear deflection accounts for the excess; band 0.95–1.08).
- **Phase B — system:** 8 m x 9 m thick-shell deck (t=0.2 m) on W18x35 beams @3 m
  (span X) + W24x94 girder lines at x=0/8 (span Y) + 4 W14x90 columns (H=3.5 m,
  fixed bases, added via new add_columns op). piles=[] so columns are the only
  supports. Saved: P005 `outputs/deck_bgc_check.sdb` (+ `plate_ss_check.sdb`).
  - Sum base reactions per case vs hand statics: LL 345.60, SDL 144.00,
    DEAD 405.15 kN — **all exact** (steel SW from AISC areas x 76.9729 kN/m3).
  - 4 column base reactions each 86.40 kN (= total/4 by double symmetry), sum exact.
  - Member LL moments vs tributary stiffness references solved with **src.fea**
    (refs themselves asserted == closed form): beam max M3 31.04 kN*m @ midspan vs
    115.2 tributary ref (ratio 0.269), girder mid M3 106.68 vs 172.8 (ratio 0.617).
  - **KEY INSIGHT:** the 0.2 m slab strip is bending-stiffer than the W18x35 under it
    (EI_slab(3m)=49.7e3 vs EI_beam=42.5e3 kN*m2, same node plane, no composite
    offset) → the shell legitimately carries most of the tributary moment. First
    band [0.35,1.15] failed at 0.269; corrected to [0.10,1.15] with the EI share
    printed. Beam-shell connectivity proven separately: shared midspan joint
    deflects 8.23 mm < beam-alone fea ref 18.09 mm.

## Files changed
- P005: `src/backend/services/sap2000/operations/modeling.py` (new),
  `operations/__init__.py` (registry), `services/sap2000/builder.py` (LL case +
  beam name mapping) — commit e24561b, pushed.
- P006: `scripts/check_thick_shell_deck.py` (new), this session log.

## Still open / deferred
- Unchanged from session 2 list above (MCP tool wrapping of `/op/*`, remaining P005
  ops, FEA Phase 1 remainder, Ch15 P9, NAFEMS paths A/C).
- Shell internal forces (AreaForceShell) op would allow a full midspan section-cut
  moment balance (beams + slab = 345.6 kN*m static total) — good next op.

## Resume point
SAP2000 left RUNNING with `deck_bgc_check.sdb` loaded; P005 server on :8000.
Goal "confirm fea model + thick shell deck check" is COMPLETE (18/18 live checks).

---

# Session 4 (2026-07-04, after /clear): sap_* MCP bridge + FEA Phase 1 COMPLETE

**User directives this session:** (1) IMPERIAL UNITS ONLY from now on — kips, ft, in,
ksi, psf; all new SAP2000 models in kip_ft; existing SI textbook benchmarks stay.
(2) Goal pivot mid-session: finish the next phase of the FEA core engine.

## 1. SAP2000 bridge tools (P006 commit f46ebc2, pushed)
- `src/agent/sap2000_tools.py` — 15 `sap_*` junior-se MCP tools calling P005's REST
  API over httpx (no new dep; mcp already requires it): status/connect/close/
  build-from-json + all 10 `/op/*` operations 1:1. Imperial defaults baked in
  (`add_columns` takes `fc_ksi`, kcf unit weight, kip_ft unit_system). Row output
  truncated at 300 with a re-query hint. `SAP2000_API_URL` env override.
- Registered in `src/mcp_server.py` via `sap2000_tools.register(mcp)`.
- `tests/test_sap2000_tools.py` — 15 unit tests via httpx.MockTransport (HTTP
  contract, error surfacing incl. server-down message, truncation, registration).
- **NOT integration-tested live** (deferred on the goal pivot) — do not claim
  otherwise. Deferred task: run a kip_ft model end-to-end through the bridge.

## 2. FEA engine Phase 1 REMAINDER — ALL DONE (P006 commit f364b9e, pushed)
Every open docs/fea_engine_plan.md Phase 1 item:
- **RCM numberer** (`numberer.py`): deterministic reverse Cuthill-McKee (degree
  then tag tie-breaks; isolated nodes last); `Model.numberer("RCM")`.
- **Load cases/combos** (plan §10.1): `Domain.set_active_patterns()` + `Domain.reset()`;
  `Model.analyze_cases({"D": 1, "L": 2})` solves each case in isolation and snapshots
  `combos.CaseResults`; `combos.combine()` (linear superposition — documented as
  invalid once nonlinear phases land) and `combos.envelope()` with governing-case
  name tracking per component.
- **Tier-2 archetypes** (`archetypes.py`): simply_supported_beam / cantilever /
  continuous_beam / portal_frame; mesh nodes auto-inserted at point-load positions
  (nodally exact for Bernoulli); key_nodes/support_nodes handles; float-tolerant
  station dedup (raw set() dedup could create ~1e-15 zero-length elements).
- **Section property tool** (`sections/properties.py`): rect/circle/polygon
  composites with holes → A, centroid, Ix/Iy/Ixy, principal I1/I2/θp, rx/ry, J,
  shear areas, `to_elastic_section()`. J: exact for single solid rect/circle;
  composite = thin-walled OPEN sum flagged approximate; ZERO for closed cells
  (holes) and polygons. **Two real bugs caught by tests:** rotated-shape Ixy sign
  (tall rect tilted CCW ⇒ mass in quadrants II/IV ⇒ Ixy<0; transform is
  −(Ix0−Iy0)sc) and a bogus open-sum J for holed (closed-cell) sections.
- **`solve_model` MCP tool** + `src/fea/json_solve.py`: Tier-1 JSON (nodes/sections/
  materials/elements incl. releases+offsets/nodal+element loads) → displacements,
  reactions, end forces, and the §15 global statics gate (ΣFx, ΣFy AND ΣMz about
  origin; output says DO NOT TRUST on failure).
- 30 new tests, all imperial closed-form (5wL⁴/384EI, PL³/48EI, PL³/3EI, wL⁴/8EI,
  two-span 3wL/8 / 10wL/8, portal mesh-invariance n=1 vs n=4, propped cantilever,
  RCM≡Plain, W12X26 3-plate vs catalogue ≤2.5%, right-triangle Ixy=−b²h²/72).
- **Repo: 256 tests, all green.** Phase 1 of the FEA engine is COMPLETE per plan.

## Still open / deferred (next session)
- Live integration test of the sap_* bridge (kip_ft model, hand-statics checks).
- Remaining P005 ops: frame release/offset assignment, AISC section assignment,
  custom axle trains; AreaForceShell extraction (section-cut moment balance).
- FEA Phase 2 kickoff per plan: MP constraints/transformation handler (native skew
  nodal axes), MITC4 shell, solids, mesher+patch tests, solver upgrade.
- NAFEMS paths A (dynamics) + C (plane stress); Ch15 P9 textbook discrepancy;
  ACI 318 textbook benchmark; Reference/ + template docs still untracked in git.

## Resume point
FEA Phase 1 done (f364b9e). Next: either FEA Phase 2 kickoff (write the concrete
Phase 2 plan first, per the plan's "expand at kickoff" rule) or the deferred sap_*
live integration test. All new work in IMPERIAL units.

---

# Session 5 (2026-07-04, after /clear): sap_* bridge LIVE integration test — PASS

**Goal (user choice on resume):** close the deferred live verification of the 15
`sap_*` MCP bridge tools before building anything else on top of them.

## What was run — `scripts/test_sap_bridge_live.py` (new), 30/30 checks PASS

Real end-to-end stack, exactly as Claude Code uses it: **MCP stdio client (the
script) → `src/mcp_server.py` (FastMCP) → sap_* tool → httpx → P005 REST →
SAP2000 COM**. This also covers the MCP protocol layer the mock-transport unit
tests could not (this Claude Code session's server snapshot predates the sap_*
registration, so the script spawns its own server instance — no restart needed).

Model (IMPERIAL kip_ft): 30x24 ft concrete deck (t=6 in, 4 ksi, 0.150 kcf,
2 ft mesh → 180 shells) on 4 W24X94 girders (X-span, y=0/8/16/24) + 8 W14X90
columns (12 ft, fixed bases) via `sap_add_columns`; SDL 20 psf, LL 50 psf;
piles=[]. Saved: P005 `outputs/sap_bridge_live_kipft.sdb`.

Key verified results (script-computed expectations, no hand numbers):
- 15/15 sap_* tools registered over MCP (28 tools total on the server).
- Base reactions: LL 36.000 and SDL 14.400 kips EXACT; DEAD 73.9675 kips ==
  slab 54.00 + girders 11.311 + columns 8.657 (AISC A/144 × **0.490 kcf** —
  confirms the kip_ft A992 default density; ratio 1.00000).
- Joint reactions (LL): 8 restrained joints, ΣF3 = 36.000000; double-symmetry
  groups exact (corners 2.8000 ×4, interior lines 6.2000 ×4).
- Cross-op consistency: column axial P from `sap_frame_forces` == its base
  reaction F3 from `sap_joint_reactions` (2.799995, compression negative).
- `sap_define_load_combos` D=[DEAD,SDL], L=LL → LC1/LC2 + LRFD-ENV; factored
  reaction sums exact: LC1 = 1.4D = 123.7145, LC2 = 1.2D+1.6L = 163.6410.
- `sap_find_joints` exact base-joint match; deck center U3 = −0.0507 in (down);
  `sap_run_analysis` DEAD/SDL/LL finished; `sap_modal_periods` returned modes
  (SAP's default MODAL case ran too).

## Gotchas recorded
- `/api/sap2000/build-from-json` response is `{"status": "success", "report":
  {...}}` — girder names live in `report["frames"]` ("requested -> actual"),
  areas in `report["areas"]`. First run failed 2 checks by reading top-level.
- Combo reaction rows can carry Max/Min step rows → sum per-joint max
  (all equal for linear-additive combos of static cases).

## Status: the sap_* bridge is now LIVE-VERIFIED (was: unit-tested only).

## Still open / deferred (unchanged otherwise)
- FEA Phase 2 kickoff (write concrete Phase 2 plan first).
- Remaining P005 ops: frame release/offset assignment, AISC section assignment,
  custom axle trains, AreaForceShell extraction.
- NAFEMS paths A (dynamics) + C (plane stress); Ch15 P9 discrepancy;
  ACI 318 textbook benchmark; Reference/ + template docs untracked.

## Resume point
SAP2000 left RUNNING with `sap_bridge_live_kipft.sdb`; P005 server on :8000.
Next: FEA Phase 2 kickoff (plan first) or next P005 operation.

---

# Session 6 (2026-07-04, after /clear): FEA Phase 2 plan + Step 1 (MP constraints) — DONE

## 1. Phase 2 concrete plan — written, APPROVED by engineer, committed (87bd590)
`docs/fea_phase2_plan.md` — 8 steps: (1) MP constraints/transformation handler + native
skew axes, (2) isoparam machinery + plane-stress Quad4 → NAFEMS LE1, (3) structured
conforming mesher + quality gates + edge stitching, (4) 3D frame 6-DOF upgrade,
(5) MITC4 shell, (6) Hex8/Tet4 solids, (7) sparse assembly + factorize-once + optional
CHOLMOD, (8) section cuts + generalized displacements. Phase gate: imperial
deck-on-frames model vs the SAP2000 thick-shell numbers + statics. Engineer approved
via prompt; status flipped to approved in the frontmatter.

## 2. Step 1 COMPLETE (commit 456ba16, pushed) — MP constraints + skew nodal axes
- **Core design:** `AnalysisModel` now resolves every node's GLOBAL displacement
  components to `u = g + T·û` over the free equations (g = prescribed SP values;
  T = identity / nodal-axes rotation / MP coupling). Transformation method:
  K̂ = TeᵀKeTe assembled per element; loads on slaved DOFs transfer via Tᵀ;
  slaved DOFs get no equation. **Elements untouched** — they still read/scatter
  global components. `_push_trials` SETS trial disp from (g, T, accumulated û)
  each update (replaces the old per-DOF increment scatter).
- `constraints/mp.py`: MP_Constraint (u_c = C·u_r, global components), equal_dof,
  rigid_link ("beam"/"bar", 2D 3-DOF), TransformationHandler. Hygiene raises: double
  slave, slave+SP, chains, skew-on-slaved-node. rigid_diaphragm deferred to Step 4
  (needs 3D 6-DOF nodes; 2D story constraint = equal_dof on ux, tested).
- Skew axes: `Model.node(tag, x, y, axes=45.0)` (2D angle or full matrix);
  fix()/equations live in the rotated frame; `node_reaction_local()` reads the
  inclined-support reaction. **Ch14 14-15/16 now solves natively** — matches
  published answers AND PlaneTruss skew oracle to 1e-9 (rotated-model test kept).
- PlainHandler rejects MP models; `Model.analyze()` auto-picks TransformationHandler.
- `tests/fea/test_mp_constraints.py` — 17 tests (splice==merged mesh, load-on-slave,
  rigid link == Frame offset closed form, story shear split, skew benchmark,
  transformed-K̂ symmetry + zero reduced residual at solved state, statics, hygiene).
- **Repo: 273 tests, all green** (was 256). Full `pytest -q` run this session.
  (Ch15 standalone scripts NOT re-run — they use src/calcs, untouched.)

## NEXT TASK (user directive, queued this session): SAP2000 moving-load verification
User: "work on moving load case. I am not confident the moving load api control for
SAP2000 works." The P005 moving-load op (truck_type P13/HL-93/HS20, commit a560225)
was verified once on 2026-07-03, but the user wants it re-checked/hardened. Suggested
approach: start P005 server + SAP2000, build a kip_ft bridge deck via the sap_* MCP
bridge or REST, run a moving-load case end-to-end (lane, vehicle, MOVE1 envelope),
read back envelope forces, and cross-check against an independent reference (e.g.
src.fea influence-line hand calc for a simple span, or textbook HS20 envelope values).
Investigate whatever the user saw failing — ask them what symptom they hit first.

## Still open / deferred (otherwise unchanged)
- FEA Phase 2 Step 2 next (isoparam + Quad4 + patch test), per approved plan.
- Remaining P005 ops: frame release/offset assignment, AISC section assignment,
  custom axle trains, AreaForceShell. NAFEMS paths A/C; Ch15 P9; ACI 318 benchmark;
  Reference/ + template docs untracked.

## Resume point
Say: "resume — moving load verification" (priority, user-queued) or "resume — Phase 2
step 2". Phase 2 Step 1 is DONE and pushed (456ba16).
